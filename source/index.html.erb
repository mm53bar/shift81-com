---
title: Posts
---

<section class="content">
  <ul class="listing">
<% page_articles.each_with_index do |article, i| %>
    <li>
      <span><%= article.date.strftime('%B %e, %Y') %></span> <%= link_to article.title, article %></a>
    </li>
<% end %>
  </ul>
</section>

